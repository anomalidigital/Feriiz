# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Logs
*.log

# Node (if added later)
node_modules/
package.json
package-lock.json
yarn.lock

# Local working files
trash/
cleanup.bat
server.js
Forgot password/

# Print reference & test renders (local only — large PDFs, not part of the site)
Print PDF simulasi/

# Backend-shaped dummy dataset (14MB, not wired into any page yet).
# Remove this line once a page actually fetches it.
feriiz-backend-dummy-data.json

# Environment
.env
.env.local

# AI assistant context (keep local, not in repo)
CLAUDE.md
.claude/
